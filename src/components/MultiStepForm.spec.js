import MultiStepForm, { reducer } from './MultiStepForm';

describe('Reducer tests', () => {
  const mockState = {
    currentStep: 1,
    allStepsData: {},
  };

  it('Should increase current step', () => {});
  it('Should decrease current step', () => {});
  it('Should add new step data', () => {});
  it('Should remove further step data if current step data changed', () => {});
  it('Should not remove further step data if current step data not changed', () => {});
});

describe('Component tests', () => {
  it('Should blabla', () => {});
});
